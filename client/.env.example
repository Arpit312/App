# For local development, replace this with your machine's local IP address (e.g. 192.168.1.10)
# Android emulator / iOS physical device cannot resolve 'localhost' to your host machine.
EXPO_PUBLIC_API_URL=http://192.168.1.20:5000/api
